<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Kayıt Ol</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
      rel="stylesheet"
    />
    <style>
      html,
      body {
        height: 100%;
        margin: 0;
        padding: 0;
      }

      body {
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
        font-family: "Roboto", sans-serif;
      }
      button {
        font-family: inherit;
        font-size: 0.9rem;
      }
      .register-form-wrapper {
        width: 25rem;
        height: 31.25rem;
        background-color: #f0f0f0;
        border: 1px solid gray;
        border-radius: 8px;
        padding: 1rem;
        margin: 3rem auto;
        color: gray;
      }

      .register-form-wrapper #registerForm {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 0;
      }

      .register-form-wrapper h1 {
        color: #9747ff;
        text-align: center;
      }

      .register-form-wrapper input {
        margin: 0.45rem;
        display: block;
        width: 90%;
        outline: none;
        border: 1px solid #dee0ea;
        padding: 0.5rem 0.5rem 0.5rem 0.938rem;
      }

      .register-form-wrapper input:focus {
        border-color: grey;
      }

      .register-form-wrapper .register-form-button {
        display: block;
        margin: 2rem auto;
        color: white;
        padding: 0.75rem;
        border-radius: 5px;
        background-color: #9747ff;
        border: none;
      }
      .register-form-wrapper .register-form-buttonhover {
        background-color: #7637c9;
      }
      .log-in-button {
        background-color: transparent;
        color: #9747ff;
        border: none;
        display: block;
        margin: 1rem auto;
        cursor: pointer;
        font-weight: 600;
      }
      .log-in-button:hover {
        color: #7637c9;
      }
    </style>
  </head>
  <body>
    <div class="register-form-wrapper">
      <h1>Kayıt Formu</h1>
      <form id="registerForm" action="/user/create" method="POST">
        <div>
          <label for="name">Adınız:</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div>
          <label for="email">E-posta:</label>
          <input type="email" id="email" name="email" required />
        </div>
        <div>
          <label for="password">Şifre:</label>
          <input type="password" id="password" name="password" required />
        </div>
        <input type="hidden" name="isSystemAdmin" value="false" />
        <button type="submit" class="register-form-button">Kayıt Ol</button>
      </form>
      <button class="log-in-button" onclick="window.location.href='/login'">
        Hesabın var mı? Giriş Yap
      </button>
    </div>
  </body>
</html>
