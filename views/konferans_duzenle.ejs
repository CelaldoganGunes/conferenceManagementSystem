<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yeni Konferans Oluştur</title>
    <style>
        header {
            background-color: #f8f9fa;
            padding: 10px 0;
            margin-bottom: 20px;
        }
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            text-align: center;
        }
        nav ul li {
            display: inline;
            margin-right: 10px;
        }
        nav ul li a {
            text-decoration: none;
            color: #000;
            padding: 5px 10px;
            border-radius: 5px;
            background-color: #007bff;
            color: white;
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/tum_konferanslar">Tüm Konferanslar</a></li>
                <li><a href="/katildigim_konferanslar">Katıldığım Konferanslar</a></li>
                <li><a href="/olusturdugum_konferanslar">Oluşturduğum Konferanslar</a></li>
                <li><a href="/yazilarim">Yazılarım</a></li>
                <li><a href="#">İncelemelerim</a></li>
                <li><a href="/cikis" class="logout-button">Çıkış Yap</a></li>
            </ul>
        </nav>
    </header>

    <h1>Konferans Düzenle</h1>
    <form action="/conference/update/<%= conference._id %>" method="POST">
        <div>
            <label for="name">Konferans Adı:</label>
            <input type="text" id="name" name="name" value="<%= conference.name %>" required>
        </div>
        <div>
            <label for="address">Adres:</label>
            <input type="text" id="address" name="address" value="<%= conference.address %>" required>
        </div><div>
            <label for="startDate">Başlangıç Tarihi:</label>
            <input type="date" id="startDate" name="startDate" value="<%= conference.startDateString %>" required>
        </div>
        <div>
            <label for="endDate">Bitiş Tarihi: </label>
            <input type="date" id="endDate" name="endDate" value="<%= conference.endDateString %>" required>
        </div>
        <div>
            <label for="attendeeLimit">Maksimum Katılımcı Limiti:</label>
            <select id="attendeeLimit" value="<%= conference.attendeeLimit %>" name="attendeeLimit" required>
                <% for (let i = 50; i <= 100; i++) { %>
                    <% if (i === conference.attendeeLimit) { %>
                        <option value="<%= i %>" selected><%= i %></option>
                    <% } else { %>
                        <option value="<%= i %>"><%= i %></option>
                    <% } %>
                <% } %>
            </select>
            
        </div>


        <button type="submit">Kaydet</button>
    </form>

    <h1>Katılımcıları Düzenle</h1>
    <form id="myForm" action="/conference/<%= conference._id %>/user/<%= user._id %>/role/0/1" method="GET">
        <div>
            <label for="attendee">Katılımcı Seç:</label>
            <select id="attendee" name="attendee">
                <% if (conference.attendeeNameArray && conference.attendeeNameArray.length > 0) { %>
                    <% for (let i = 0; i < conference.keys.length; i++) { %>
                        <option value="<%= conference.keys[i] %>"> <%= conference.attendeeNameArray[i] %> - <%= conference.roleNames[conference.roles[i]] %> </option>
                    <% } %>
                <% } else { %>
                    <option disabled selected>No attendees</option>
                <% } %>
            </select>
        </div>
        <div>
            <label for="role">Rol Seç:</label>
            <select id="role" name="role">
                <option value="1">Attendee</option>
                <option value="2">Reviewer</option>
                <option value="3">Author</option>
                <option value="4">Conf Admin (Hocam lütfen bunu seçmeyin)</option>
            </select>
        </div>
        <button type="submit">Kaydet</button>

    </form>

</body>
</html>
