<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tüm Konferanslar</title>
    <style>
        header {
            background-color: #f8f9fa;
            padding: 10px 0;
            margin-bottom: 20px;
        }
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            text-align: center;
        }
        nav ul li {
            display: inline;
            margin-right: 10px;
        }
        nav ul li a {
            text-decoration: none;
            color: #000;
            padding: 5px 10px;
            border-radius: 5px;
            background-color: #007bff;
            color: white;
        }
        .conference-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            margin-top: 20px;
        }
        .conference-item {
            width: 30%;
            margin-bottom: 20px;
            padding: 10px;
            border: 20px solid #ccc;
            border-radius: px;
        }
        .conference-item h2 {
            margin-top: 0;
        }
        .conference-item button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/tum_konferanslar">Tüm Konferanslar</a></li>
                <li><a href="/katildigim_konferanslar">Katıldığım Konferanslar</a></li>
                <li><a href="/olusturdugum_konferanslar">Oluşturduğum Konferanslar</a></li>
                <li><a href="/yazilarim">Yazılarım</a></li>
                <li><a href="#">İncelemelerim</a></li>
                <li><a href="/cikis" class="logout-button">Çıkış Yap</a></li>
            </ul>
        </nav>
    </header>
    
    <main>
        <h1>Tüm Konferanslar</h1>
        <button onclick="window.location.href='/yeni_konferans'">Yeni Konferans Oluştur</button>
        <div class="conference-grid">
            <% conferences.forEach(conference => { %>
                <div class="conference-item">
                    <h2><%= conference.name %></h2>
                    <p>ID: <%= conference._id %></p>
                    <p>Başlangıç Tarihi: <%= conference.startDate %></p>
                    <p>Bitiş Tarihi: <%= conference.endDate %></p>
                    <p>Adres: <%= conference.address %></p>

                    <% if (!conference.attendeeList.get(user._id)) { %>
                        <form action="/conference/<%= conference._id %>/user/<%= user._id %>/role/1/0" method="GET">
                            <input class="submit" type="submit" placeholder="LOGIN" value="Attend">
                        </form>
                    <% } %>
                </div>
            <% }); %>
        </div>
    </main>
</body>
</html>
